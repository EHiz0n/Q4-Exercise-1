
        function round() {
            var a = document.getElementById('a').value;
            var b = document.getElementById('b').value;
            window.alert(Math.round(a));
        }

        function power() {
            var a value = document.getElementById("a").value;
            var b value = document.getElementById("b").value;
            window.alert(Math.pow(a,b));
        }

        function square() {
            var a value = document.getElementById("a").value;
            var b value = document.getElementById("b").value;
            window.alert(Math.sqrt(a));
        }

        function checkEquality() {
            var a value = document.getElementById("a").value;
            var b value = document.getElementById("b").value;
            window.alert(a == b ? "A is equal to B" : "A is NOT equal to B");
        }